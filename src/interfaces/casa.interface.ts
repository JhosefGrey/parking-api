export interface CasaInterface {
    direccion: string,
    codigo: string,
    bloqueId: any
}

export interface CasaUpdate {
    idCasa: string,
    direccion: string,
    codigo: string,
    bloqueId: string
}